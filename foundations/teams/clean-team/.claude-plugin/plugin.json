{
  "name": "clean-team",
  "version": "4.1.0",
  "description": "An 8-agent pipeline in two phases — Phase 1: Analyze (organize → format → audit), checkpoint for user approval, Phase 2: Refactor (test → plan → challenge → refactor → verify). One command with three modes: full pipeline, audit-only (read-only), and resume (auto-detected from existing audit report).",
  "agents": [
    "./agents/organizer.md",
    "./agents/formatter.md",
    "./agents/auditor.md",
    "./agents/tester.md",
    "./agents/planner.md",
    "./agents/challenger.md",
    "./agents/refactorer.md",
    "./agents/verifier.md"
  ]
}
