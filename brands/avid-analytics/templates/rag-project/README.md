# [Client Name] — RAG Project

TODO: Build out this template scaffold.

## Structure

```
sources/        → Raw documents to be indexed
ingestion/      → Chunking, cleaning, embedding scripts
store/          → Vector store configuration
retrieval/      → Query and retrieval API
eval/           → Test questions, expected answers, accuracy scoring
docs/           → Architecture decisions, configuration guide
config/         → Model settings, chunk sizes, embedding config
```

## Getting Started

1. Copy this template to a new project directory
2. Update this README with client name and context
3. Collect source documents into `sources/`
4. Configure chunking strategy in `config/`
5. Run eval harness after initial indexing
