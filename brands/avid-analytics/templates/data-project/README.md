# [Client Name] — Data Project

TODO: Build out this template scaffold.

## Structure

```
raw/            → Source data as-is (never modify)
staged/         → Cleaned, validated, intermediate
curated/        → Final tables ready for reporting/analytics
pipelines/      → ETL scripts and orchestration
docs/           → Data dictionary, lineage maps, quality reports
config/         → Connection strings, environment config
```

## Getting Started

1. Copy this template to a new project directory
2. Update this README with client name and context
3. Run `/audit-data` against existing data sources
4. Use `data-documenter` agent to generate initial documentation
